version: '3.8'

services:
  backend:
    build: ./backend
    container_name: zksentinel_backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app
      - ./circuits:/app/circuits
    environment:
      - ENV=development